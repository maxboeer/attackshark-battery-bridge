# AUR packaging

This directory contains the Arch Linux packaging for `attackshark-battery-bridge`.

The package builds the daemon from a tagged release source archive and installs:

- the `attackshark-battery-bridge` executable
- the systemd service unit
- the default configuration file

The AUR metadata in this directory is kept in sync with GitHub releases.
